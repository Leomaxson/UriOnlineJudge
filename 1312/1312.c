#include <stdlib.h>
#include <stdio.h>

int main() {
  int numLinhas;

  // Lê o tamanho da primeira instância.
  scanf("%d", &numLinhas);
  while(numLinhas > 0) {


    // Lê o tamanho da próxima instância.
    scanf("%d", &numLinhas);
  }
  return 0;
}